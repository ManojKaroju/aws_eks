# Install eksctl on your windows
1. choco install -y eksctl 

# Verify the Installation
2. eksctl version

#This will create EKS cluster with Master Plane and without Worker Nodes.
eksctl create cluster --name=eks-demo --region=us-east-1 --zones=us-east-1a,us-east-1b --without-nodegroup 

#To create the Worker Nodes for the eks-demo cluster in private network
eksctl create nodegroup -cluster=eks-demo --region, --nodetype=t2.micro --node-private-networking
